security:
  basic:
    enabled: false
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/xmxkkk/spring-cloud-repo
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:10000/eureka/,http://127.0.0.1:10001/eureka/
